name: CI

on:
  push:
    branches: [main, "v1.*"]
  pull_request:
    branches: [main, "v1.*"]

permissions:
  contents: read

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install test dependencies
        run: |
          pip install pytest pyyaml

      - name: Run pre-commit
        run: |
          pip install pre-commit
          pre-commit run --all-files

      - name: Run tests
        run: |
          PYTHONPATH=. pytest --tb=short

      - name: Run LLM expert review evals
        run: |
          for id in 1 2 3; do
            echo "Running eval case $id..."
            python -m evals.runner --eval-id $id --output-text "mock output for schema validation"
          done

  scene-e2e:
    name: Scene E2E Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pytest pyyaml

      - name: Run scene E2E tests
        env:
          FEISHU_AM_TEST_LIVE: "true"
          FEISHU_AM_BOT_APP_ID: ${{ secrets.FEISHU_AM_BOT_APP_ID }}
          FEISHU_AM_BOT_APP_SECRET: ${{ secrets.FEISHU_AM_BOT_APP_SECRET }}
          FEISHU_AM_BASE_TOKEN: ${{ secrets.FEISHU_AM_BASE_TOKEN }}
        run: |
          PYTHONPATH=. python -m pytest tests/test_scene_e2e/ -v
