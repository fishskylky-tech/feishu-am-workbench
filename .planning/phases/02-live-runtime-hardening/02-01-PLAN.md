---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/runtime_sources.py
  - runtime/diagnostics.py
  - runtime/resource_resolver.py
  - runtime/customer_resolver.py
  - tests/test_runtime_smoke.py
  - tests/test_env_loader.py
  - STATUS.md
  - references/feishu-runtime-sources.md
autonomous: true
must_haves:
  truths:
    - Required live resources are resolved only from private runtime env-backed values.
    - Missing required private runtime inputs block live startup and surface explicit next actions.
    - Customer resolution remains deterministic and never auto-selects ambiguous matches.
  artifacts:
    - runtime/runtime_sources.py only emits env-backed runtime hints.
    - Regression tests cover missing env diagnostics and customer resolution edge cases.
    - Docs describe .env as convenience-only, not a co-equal truth source.
  key_links:
    - FOUND-02 -> runtime/env_loader.py + runtime/runtime_sources.py
    - LIVE-01/LIVE-02 -> runtime/resource_resolver.py + runtime/diagnostics.py
    - LIVE-03 -> runtime/customer_resolver.py + regression tests
---

# Phase 2 Plan 01: Harden Live Runtime Ground Truth

<objective>
Make live runtime startup fail closed around private runtime input, show plain-language startup diagnostics, and preserve deterministic customer lookup without widening into broader business behavior.
</objective>

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-live-runtime-hardening/02-CONTEXT.md
@.planning/phases/02-live-runtime-hardening/02-RESEARCH.md

<tasks>
<task type="code">
  <name>Remove repo-derived runtime truth</name>
  <read_first>
    runtime/runtime_sources.py
    runtime/env_loader.py
    runtime/models.py
  </read_first>
  <files>
    runtime/runtime_sources.py
    tests/test_runtime_smoke.py
  </files>
  <action>
    Restrict runtime source loading to env-backed hints only. Remove checked-in repo document parsing from live resource resolution and update or add tests that prove repo files cannot satisfy required live resource keys.
  </action>
  <verify>
    Run the targeted unit tests covering runtime source loading and smoke behavior.
  </verify>
  <acceptance_criteria>
    - runtime/runtime_sources.py no longer reads checked-in reference docs to populate live resource hints
    - required live resource hints report env-based source_file values when present
    - tests demonstrate missing env stays missing even if repo reference files exist
  </acceptance_criteria>
</task>

<task type="code">
  <name>Make blocked startup diagnostics explicit</name>
  <read_first>
    runtime/resource_resolver.py
    runtime/diagnostics.py
    runtime/gateway.py
    tests/test_runtime_smoke.py
  </read_first>
  <files>
    runtime/diagnostics.py
    runtime/resource_resolver.py
    tests/test_runtime_smoke.py
  </files>
  <action>
    Tighten resource and diagnostic messaging so operators can clearly see available, degraded, and blocked states, along with the reason and next action when required private inputs are missing.
  </action>
  <verify>
    Run targeted tests covering missing-resource diagnostics and confirm rendered output includes conclusion, reason, and next action.
  </verify>
  <acceptance_criteria>
    - diagnostics distinguish available, degraded, and blocked states in plain language
    - blocked startup output includes the missing keys and a concrete remediation step
    - no diagnostic text implies repo-doc fallback as a valid live path
  </acceptance_criteria>
</task>

<task type="code">
  <name>Lock deterministic customer resolution coverage</name>
  <read_first>
    runtime/customer_resolver.py
    tests/test_runtime_smoke.py
  </read_first>
  <files>
    runtime/customer_resolver.py
    tests/test_runtime_smoke.py
  </files>
  <action>
    Add focused regression coverage for exact match, customer ID match, single remaining candidate, missing result, and ambiguous result. Only change resolver behavior if tests expose a direct requirement gap, and preserve the rule that ambiguity stops instead of guessing.
  </action>
  <verify>
    Run customer-resolution tests and confirm ambiguous matches remain non-resolved.
  </verify>
  <acceptance_criteria>
    - regression tests cover exact short name, customer ID, single-candidate, missing, and ambiguous cases
    - ambiguous customer results remain non-resolved
    - no broader fuzzy-matching behavior is introduced
  </acceptance_criteria>
</task>

<task type="docs">
  <name>Align docs with the hardened contract</name>
  <read_first>
    STATUS.md
    references/feishu-runtime-sources.md
    .planning/phases/02-live-runtime-hardening/02-CONTEXT.md
  </read_first>
  <files>
    STATUS.md
    references/feishu-runtime-sources.md
  </files>
  <action>
    Update the minimum documentation needed so the runtime contract says private env is authoritative, .env is convenience-only, diagnostics are operator-facing, and Phase 2 remains a narrow foundation-hardening phase.
  </action>
  <verify>
    Read the updated docs and confirm they no longer describe repo-doc fallbacks or dual authoritative source layers.
  </verify>
  <acceptance_criteria>
    - docs describe .env as convenience-only
    - docs no longer imply repo reference files can supply live runtime truth
    - doc updates stay limited to runtime-source and diagnostic contract clarification
  </acceptance_criteria>
</task>
</tasks>

<verification>
- python -m unittest tests.test_env_loader
- python -m unittest tests.test_runtime_smoke
</verification>

<success_criteria>
- [ ] Required live resources cannot be inferred from checked-in repo docs.
- [ ] Missing required private env blocks the live path and yields explicit, actionable diagnostics.
- [ ] Diagnostics use the agreed three-state vocabulary and include next-step guidance.
- [ ] Customer resolution behavior is deterministic and ambiguous results do not auto-resolve.
- [ ] Docs and tests reflect the hardened contract without widening Phase 2 scope.
</success_criteria>