---
phase: 03-core-context-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/models.py
  - runtime/semantic_registry.py
  - evals/meeting_output_bridge.py
  - tests/test_meeting_output_bridge.py
autonomous: true
requirements:
  - LIVE-04
  - WORK-01
  - MEET-01
must_haves:
  truths:
    - Meeting and post-meeting recovery does not claim grounded context before gateway resource and customer stages complete.
    - A resolved customer yields a scene-level recovery bundle over 客户主数据, 客户联系记录, and 行动计划.
    - The 3 core-table semantic contract stays narrow and live-backed rather than drifting into full-schema mirroring.
  artifacts:
    - path: runtime/models.py
      provides: typed recovery evidence, open-question, and write-ceiling contracts
    - path: evals/meeting_output_bridge.py
      provides: gateway-first scene recovery over the 3 core tables
    - path: tests/test_meeting_output_bridge.py
      provides: regression coverage for gateway-first and three-table recovery behavior
  key_links:
    - from: evals/meeting_output_bridge.py
      to: runtime/models.py
      via: typed context recovery result
      pattern: ContextRecovery|WriteCeiling
    - from: evals/meeting_output_bridge.py
      to: runtime/semantic_registry.py
      via: semantic slots used for the 3 core tables
      pattern: archive_link|meeting_note_doc
---

<objective>
Lock the Phase 3 scene contract so meeting-related flows recover context only after the live gateway runs, always use the 3 core tables for resolved customers, and expose typed recovery evidence without widening the gateway boundary.

Purpose: Establish the contract that all later archive-routing and output work will build on.
Output: Typed recovery models, a gateway-first scene recovery implementation, and regression tests for the 3 core-table path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-context-recovery/03-CONTEXT.md
@.planning/phases/03-core-context-recovery/03-RESEARCH.md
@.planning/phases/03-core-context-recovery/03-VALIDATION.md
@runtime/models.py
@runtime/semantic_registry.py
@evals/meeting_output_bridge.py
@tests/test_meeting_output_bridge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define typed recovery contracts and failing regression expectations</name>
  <files>runtime/models.py, tests/test_meeting_output_bridge.py</files>
  <behavior>
    - Test 1: meeting recovery output stays `context-limited` when gateway customer resolution is missing or ambiguous.
    - Test 2: resolved customers produce a typed recovery bundle that can carry used sources, missing sources, open questions, and write ceiling separately.
    - Test 3: fallback-related write-ceiling decisions are represented explicitly instead of being inferred from free-form text.
  </behavior>
  <action>Introduce narrow dataclasses or typed structures in `runtime/models.py` for scene-level recovery evidence, candidate conflicts, open questions, and write ceiling so Phase 3 logic stops passing loosely coupled dicts and strings around. Add or update `tests/test_meeting_output_bridge.py` first to assert the gateway-first contract and the existence of typed recovery fields per D-08, D-09, D-11, and D-12. Keep the types scene-facing; do not move business context assembly into `runtime/gateway.py`.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge -q</automated>
  </verify>
  <done>Typed recovery/result contracts exist, tests describe the gateway-first and confidence-aware behavior, and no production code outside the Phase 3 scene surface is widened prematurely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement gateway-first three-core-table recovery</name>
  <files>evals/meeting_output_bridge.py, runtime/semantic_registry.py, tests/test_meeting_output_bridge.py</files>
  <behavior>
    - Test 1: a resolved customer always triggers reads from 客户主数据, 客户联系记录, and 行动计划 before the scene reports grounded context.
    - Test 2: missing contact or action rows keep the run auditable via missing-sources fields instead of silently downgrading the case.
    - Test 3: the scene keeps the semantic contract narrow, using only the slots needed for the 3 core tables.
  </behavior>
  <action>Refactor `evals/meeting_output_bridge.py` to consume the typed recovery contracts, execute the three-core-table read path after successful gateway/customer resolution, and emit an auditable scene-level recovery bundle. Adjust `runtime/semantic_registry.py` only if Phase 3 needs narrow alias or slot clarifications to keep the 3-table contract live-backed per WORK-01. Preserve the thin-runtime rule: `runtime/gateway.py` should still stop at resource resolution, customer resolution, and write safety, while the scene layer decides which Base rows to read and how to summarize them.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge -q</automated>
  </verify>
  <done>Meeting recovery is gateway-first, always uses the 3 core tables for resolved customers, and reports structured recovery evidence without expanding into full-schema or gateway-side context assembly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript -> scene recovery | Untrusted meeting text must not be treated as grounded customer context before gateway stages run |
| scene recovery -> Base query backend | Customer-scoped reads must stay tied to the resolved customer ID and the minimal semantic contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | evals/meeting_output_bridge.py | mitigate | Require gateway/customer resolution to complete before any grounded `completed` or `partial` context status is emitted; unresolved or ambiguous customers stay `context-limited` |
| T-03-02 | T | runtime/models.py | mitigate | Use explicit typed recovery/write-ceiling structures so later code cannot silently reinterpret free-form text as safe recovery evidence |
| T-03-03 | E | evals/meeting_output_bridge.py | mitigate | Constrain Phase 3 reads to the 3 core tables and preserve scene-level orchestration so the gateway does not gain implicit context-assembly power |
</threat_model>

<verification>
- ./.venv/bin/python -m unittest tests.test_meeting_output_bridge -q
</verification>

<success_criteria>
- [ ] Phase 3 has typed recovery and write-ceiling contracts rather than free-form dict conventions.
- [ ] A resolved customer always yields a gateway-first recovery path over 客户主数据, 客户联系记录, and 行动计划.
- [ ] Missing rows stay visible as audit evidence instead of implicit fallback.
- [ ] The semantic contract for the 3 core tables remains small, live-backed, and test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-context-recovery/03-01-SUMMARY.md`
</output>
