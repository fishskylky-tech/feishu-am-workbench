---
phase: 03-core-context-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - runtime/live_adapter.py
  - evals/meeting_output_bridge.py
  - tests/test_meeting_output_bridge.py
autonomous: true
requirements:
  - LIVE-04
  - WORK-03
  - MEET-02
must_haves:
  truths:
    - Archive and meeting-note routing prefers explicit links but can use constrained fallback discovery when links are absent or stale.
    - Duplicate or conflicting archive / meeting-note candidates are surfaced explicitly and lower trust in the recovered context.
    - Final meeting output exposes stable audit fields including write ceiling and open questions.
  artifacts:
    - path: runtime/live_adapter.py
      provides: thin raw candidate discovery for archive and meeting-note routing
    - path: evals/meeting_output_bridge.py
      provides: confidence-aware fallback routing and final audit output rendering
    - path: tests/test_meeting_output_bridge.py
      provides: regression coverage for unique fallback, conflict surfacing, and recommendation-only downgrade
  key_links:
    - from: runtime/live_adapter.py
      to: evals/meeting_output_bridge.py
      via: raw candidate listings and evidence used for scene-side ranking
      pattern: archive|meeting|candidate
    - from: evals/meeting_output_bridge.py
      to: tests/test_meeting_output_bridge.py
      via: audit fields and write ceiling regressions
      pattern: 写回上限|开放问题|已使用资料
---

<objective>
Add constrained archive and historical meeting-note routing to the Phase 3 scene flow, then finalize a stable audit output contract that exposes confidence, conflicts, write ceiling, and open questions.

Purpose: Deliver the trust loop that makes richer context recovery useful without weakening safety.
Output: Thin candidate-discovery helpers, confidence-aware fallback routing, and final auditable meeting output with regression coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-context-recovery/03-CONTEXT.md
@.planning/phases/03-core-context-recovery/03-RESEARCH.md
@.planning/phases/03-core-context-recovery/03-VALIDATION.md
@references/meeting-context-recovery.md
@references/meeting-live-first-policy.md
@references/meeting-output-standard.md
@references/customer-archive-rules.md
@runtime/live_adapter.py
@evals/meeting_output_bridge.py
@tests/test_meeting_output_bridge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add constrained archive and meeting-note candidate discovery</name>
  <files>runtime/live_adapter.py, evals/meeting_output_bridge.py, tests/test_meeting_output_bridge.py</files>
  <behavior>
    - Test 1: explicit archive or meeting-note links are used first and do not trigger broader fallback search.
    - Test 2: when links are absent, folder-scoped fallback can return a single high-confidence candidate using customer-scoped evidence.
    - Test 3: duplicate or conflicting candidates remain explicit instead of collapsing to one hidden winner.
  </behavior>
  <action>Extend `runtime/live_adapter.py` with a thin raw discovery surface for archive and meeting-note candidates using current Drive folder hints and available metadata. Keep the adapter evidence-oriented: return candidate docs plus ranking signals, not scene conclusions. Then update `evals/meeting_output_bridge.py` to apply Phase 3 ranking rules per D-05, D-06, and D-07: explicit links first, constrained fallback second, conflicts surfaced as uncertainty. Add regression tests for unique fallback, duplicate/conflict, and no-safe-match cases.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge -q</automated>
  </verify>
  <done>Archive and meeting-note routing can recover a unique candidate when evidence is strong, but conflicting candidates remain visible and auditable rather than silently accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Finalize the strong audit output and confidence-based write ceiling</name>
  <files>evals/meeting_output_bridge.py, tests/test_meeting_output_bridge.py</files>
  <behavior>
    - Test 1: final scene output always shows resource status, customer result, context status, used sources, key background, missing sources, write ceiling, and open questions.
    - Test 2: low-confidence or conflicting fallback recovery downgrades write ceiling to recommendation-only.
    - Test 3: high-confidence unique fallback candidates can keep the normal downstream write path while still disclosing how the context was recovered.
  </behavior>
  <action>Update the final output builder in `evals/meeting_output_bridge.py` so the Phase 3 scene emits a stable Chinese audit frame aligned with `references/meeting-output-standard.md` and the locked decisions in `03-CONTEXT.md`. Add explicit write-ceiling and open-question rendering derived from the typed recovery result and fallback confidence. Ensure recommendation-only downgrade happens whenever recovery evidence is conflicting or incomplete per D-11 and D-12. Expand tests to prove the downgrade/keep-normal split and to keep the output contract stable for later phases.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_env_loader tests.test_runtime_smoke tests.test_meeting_output_bridge -q</automated>
  </verify>
  <done>Meeting output exposes the full audit contract, write ceiling is confidence-aware, and fallback routing no longer hides uncertainty or overstates safety.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drive/doc candidate discovery -> scene ranking | Raw folder metadata is not trustworthy enough to become grounded context without explicit evidence and conflict checks |
| scene output -> downstream write planning | Output fields like write ceiling and open questions influence later write decisions and must not overstate confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | I | runtime/live_adapter.py | mitigate | Limit fallback discovery to configured folders and customer-scoped signals so unrelated docs are not pulled into the current customer thread |
| T-03-05 | T | evals/meeting_output_bridge.py | mitigate | Surface duplicate/stale/conflicting candidates explicitly and require scene-side confidence checks before accepting fallback context |
| T-03-06 | E | evals/meeting_output_bridge.py | mitigate | Compute `写回上限` from recovery confidence and force recommendation-only when fallback evidence is conflicting or incomplete |
</threat_model>

<verification>
- ./.venv/bin/python -m unittest tests.test_meeting_output_bridge -q
- ./.venv/bin/python -m unittest tests.test_env_loader tests.test_runtime_smoke tests.test_meeting_output_bridge -q
</verification>

<success_criteria>
- [ ] Archive and meeting-note routing supports explicit-link-first plus constrained fallback search.
- [ ] Duplicate and stale candidate risks are visible in output instead of being silently masked.
- [ ] Final meeting output exposes the full audit frame, including write ceiling and open questions.
- [ ] Conflicting or low-confidence fallback recovery downgrades the scene to recommendation-only behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-context-recovery/03-02-SUMMARY.md`
</output>
