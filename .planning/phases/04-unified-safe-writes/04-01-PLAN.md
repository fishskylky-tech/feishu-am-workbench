---
phase: 04-unified-safe-writes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - runtime/models.py
  - tests/test_meeting_output_bridge.py
  - tests/test_runtime_smoke.py
autonomous: true
requirements:
  - MEET-03
  - WRITE-02
must_haves:
  truths:
    - Meeting-scene write candidates can be consolidated conservatively without losing explicit `operation`, `match_basis`, `source_context`, and `target_object` fields.
    - Confirmed write paths still preserve a full normalized result envelope internally even if default user output later becomes concise.
    - Runtime and scene regression suites describe the contract before implementation widens behavior.
  artifacts:
    - path: runtime/models.py
      provides: stable typed contract for normalized write candidates and execution-result evidence
    - path: tests/test_meeting_output_bridge.py
      provides: regression expectations for same-meeting candidate consolidation and output contract invariants
    - path: tests/test_runtime_smoke.py
      provides: regression expectations for normalized write-result fields across blocked and allowed paths
  key_links:
    - from: evals/meeting_output_bridge.py
      to: runtime/models.py
      via: normalized candidate and write-result contracts
      pattern: WriteCandidate|WriteExecutionResult|source_context
    - from: runtime/todo_writer.py
      to: runtime/models.py
      via: normalized result envelope returned to scenes
      pattern: TodoWriteResult|preflight_status|guard_status
---

<objective>
Lock the Phase 4 contract layer first so candidate normalization, result-envelope stability, and downstream safety policies are expressed as typed, test-backed runtime rules before scene or writer behavior changes.

Purpose: Give the later Phase 4 plans a stable contract surface and failing regression targets.
Output: Updated write-related model contracts plus regression expectations in meeting-scene and runtime-smoke tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unified-safe-writes/04-CONTEXT.md
@.planning/phases/04-unified-safe-writes/04-RESEARCH.md
@.planning/phases/04-unified-safe-writes/04-VALIDATION.md
@runtime/models.py
@tests/test_meeting_output_bridge.py
@tests/test_runtime_smoke.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define failing regressions for candidate consolidation and result-envelope invariants</name>
  <files>tests/test_meeting_output_bridge.py, tests/test_runtime_smoke.py</files>
  <behavior>
    - Test 1: same-meeting same-theme actions may consolidate, but every emitted candidate still carries explicit `operation`, `match_basis`, `source_context`, and `target_object`.
    - Test 2: blocked and allowed write paths both preserve the normalized result fields needed for validation, including preflight, guard, dedupe, and remote metadata or blocked reasons.
    - Test 3: concise default output cannot become an excuse to drop the underlying structured result evidence.
  </behavior>
  <action>Add or update regression cases in `tests/test_meeting_output_bridge.py` and `tests/test_runtime_smoke.py` first so Phase 4 behavior is described before implementation changes land. Encode the locked decisions from D-01 through D-07 and D-14 through D-16 as explicit assertions. Keep the tests narrow: they should pin contract behavior, not guess future UI wording or invent a generic Base write path.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge tests.test_runtime_smoke -q</automated>
  </verify>
  <done>The regression suite fails for the right reasons and clearly describes the contract that later plans must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce the stable model contract that later plans will implement against</name>
  <files>runtime/models.py, tests/test_meeting_output_bridge.py, tests/test_runtime_smoke.py</files>
  <behavior>
    - Test 1: runtime model contracts can represent consolidated candidate evidence without losing original write-routing fields.
    - Test 2: execution results keep machine-readable safety evidence even when later rendering becomes concise.
    - Test 3: the contract remains thin and object-agnostic rather than becoming a generic workflow engine.
  </behavior>
  <action>Make the minimum contract changes in `runtime/models.py` needed for Phase 4: stabilize the write-candidate and execution-result evidence surface so scene code and writer code can evolve independently in later plans. If helper fields or typed wrappers are required for concise-vs-structured output handling, add them here; if they are not required, keep the model unchanged and only codify the invariant in tests. Do not introduce speculative abstraction or a generic mutation framework.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge tests.test_runtime_smoke -q</automated>
  </verify>
  <done>The model layer gives downstream plans a stable, test-backed contract for normalized candidates and execution results, without expanding scope beyond Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meeting scene -> candidate contract | Untrusted meeting text must not collapse distinct actions or drop grounding metadata during candidate normalization |
| writer result -> user-facing output | Safety evidence must survive the transition from machine-facing result to concise default presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | runtime/models.py | mitigate | Stabilize typed contracts so later plans cannot silently change or omit required write-routing and result-evidence fields |
| T-04-02 | I | tests/test_meeting_output_bridge.py | mitigate | Add regressions that fail if same-meeting consolidation drops `match_basis`, `source_context`, or `target_object` |
| T-04-03 | R | tests/test_runtime_smoke.py | mitigate | Require blocked and allowed paths to retain normalized safety evidence so later output rendering cannot erase auditability |
</threat_model>

<verification>
- ./.venv/bin/python -m unittest tests.test_meeting_output_bridge tests.test_runtime_smoke -q
</verification>

<success_criteria>
- [ ] Phase 4 has a stable contract layer for normalized candidates and normalized write results.
- [ ] Regressions pin the required routing and audit fields before scene/runtime implementation widens behavior.
- [ ] Later plans can build on these contracts without reopening scope or adding generic abstractions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-safe-writes/04-01-SUMMARY.md`
</output>
