---
phase: 04-unified-safe-writes
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - evals/meeting_output_bridge.py
  - tests/test_meeting_output_bridge.py
  - evals/runner.py
autonomous: true
requirements:
  - MEET-03
  - WRITE-02
must_haves:
  truths:
    - Post-meeting flows emit fewer, clearer Todo candidates by consolidating only same-meeting same-theme actions.
    - Every emitted candidate still carries explicit routing and grounding fields needed by the gateway and writer.
    - Default user-facing writeback summaries become concise without sacrificing the underlying structured result contract used by tests and validation.
  artifacts:
    - path: evals/meeting_output_bridge.py
      provides: same-meeting candidate consolidation and concise scene-level writeback rendering
    - path: tests/test_meeting_output_bridge.py
      provides: regression coverage for consolidated candidate generation and concise-output behavior
    - path: evals/runner.py
      provides: validation-path access to the full structured result even when default presentation is concise
  key_links:
    - from: evals/meeting_output_bridge.py
      to: runtime/models.py
      via: normalized candidate/result contracts from Plan 01
      pattern: WriteCandidate|WriteExecutionResult|blocked_reasons
    - from: evals/runner.py
      to: evals/meeting_output_bridge.py
      via: validation assertions over structured results and rendered summaries
      pattern: write_result|candidate|output
---

<objective>
Implement the scene-layer side of Phase 4 so post-meeting flows generate better normalized Todo candidates and present concise default writeback summaries while preserving the full structured evidence required for validation.

Purpose: Improve the quality and usability of the meeting -> gateway -> Todo path without moving business judgment into the runtime foundation.
Output: Updated meeting bridge logic, regression coverage, and validation-runner compatibility for concise default output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unified-safe-writes/04-CONTEXT.md
@.planning/phases/04-unified-safe-writes/04-RESEARCH.md
@.planning/phases/04-unified-safe-writes/04-VALIDATION.md
@.planning/phases/04-unified-safe-writes/04-01-PLAN.md
@evals/meeting_output_bridge.py
@tests/test_meeting_output_bridge.py
@evals/runner.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement conservative same-meeting candidate consolidation</name>
  <files>evals/meeting_output_bridge.py, tests/test_meeting_output_bridge.py</files>
  <behavior>
    - Test 1: same-meeting same-theme action items consolidate into fewer Todo candidates instead of sentence-level fragmentation.
    - Test 2: cross-meeting or meaningfully distinct actions do not auto-merge.
    - Test 3: every consolidated candidate still exposes `operation`, `match_basis`, `source_context`, and `target_object`.
  </behavior>
  <action>Update `evals/meeting_output_bridge.py` to perform a small, auditable consolidation pass for post-meeting Todo candidates per D-01 through D-04. The consolidation rule must stay scene-local and same-meeting-only; do not push it into `runtime/todo_writer.py`. Use `tests/test_meeting_output_bridge.py` to prove that the merge heuristic is conservative and that normalized routing fields survive intact.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge -q</automated>
  </verify>
  <done>Meeting flows emit fewer, clearer candidates without losing the explicit machine-readable fields required by the gateway and writer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add concise default writeback summaries without dropping structured evidence</name>
  <files>evals/meeting_output_bridge.py, evals/runner.py, tests/test_meeting_output_bridge.py</files>
  <behavior>
    - Test 1: default scene output can render a concise natural-language writeback summary for allowed or blocked paths.
    - Test 2: validation and runner paths can still access the full structured result envelope.
    - Test 3: concise output never hides blocked reasons, dedupe decisions, or the existence of a remote object when one exists.
  </behavior>
  <action>Layer concise default rendering on top of the structured result contract in `evals/meeting_output_bridge.py` and keep `evals/runner.py` wired to structured evidence for assertions and validation review. Follow D-14 through D-16 exactly: preserve the rich internal result envelope, but allow the default user-facing path to summarize it in natural language. Avoid adding a separate presentation framework; keep the change as a thin scene/output concern.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_meeting_output_bridge tests.test_eval_runner -q</automated>
  </verify>
  <done>Default output becomes concise and easier to read, while tests and validation still rely on the unchanged structured result evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript/action extraction -> candidate consolidation | Consolidation must not merge unrelated actions or erase grounded routing metadata |
| structured result -> concise summary | Concise summaries must not suppress blocked reasons or overstate write success |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | evals/meeting_output_bridge.py | mitigate | Keep consolidation same-meeting-only and preserve explicit routing fields after merge |
| T-04-05 | I | evals/meeting_output_bridge.py | mitigate | Make concise summaries derive from the structured result envelope so blocked reasons and dedupe outcomes remain available |
| T-04-06 | R | evals/runner.py | mitigate | Keep runner assertions tied to structured result fields rather than free-form user-facing strings |
</threat_model>

<verification>
- ./.venv/bin/python -m unittest tests.test_meeting_output_bridge tests.test_eval_runner -q
</verification>

<success_criteria>
- [ ] Same-meeting same-theme actions consolidate conservatively into clearer Todo candidates.
- [ ] Normalized candidate fields remain explicit after consolidation.
- [ ] Default writeback summaries are concise, but the full structured result contract remains accessible for validation and debugging.
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-safe-writes/04-02-SUMMARY.md`
</output>
