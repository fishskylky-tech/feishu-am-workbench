---
phase: 04-unified-safe-writes
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - runtime/gateway.py
  - runtime/schema_preflight.py
  - runtime/write_guard.py
  - runtime/todo_writer.py
  - runtime/semantic_registry.py
  - tests/test_runtime_smoke.py
autonomous: true
requirements:
  - WRITE-01
  - WRITE-02
  - WRITE-03
must_haves:
  truths:
    - Every write-ready path still goes through schema preflight and write guard before execution.
    - Todo create/update remains the only fully hardened confirmed execution surface in Phase 4.
    - Customer-master updates are split cleanly into a tiny allowlisted low-risk fact slice versus recommendation-only high-risk or judgment-heavy fields.
  artifacts:
    - path: runtime/schema_preflight.py
      provides: explicit live-schema blocking and allowlist-aware field resolution for write-ready paths
    - path: runtime/write_guard.py
      provides: object-level enforcement for low-risk versus guarded customer-master updates
    - path: tests/test_runtime_smoke.py
      provides: regression coverage for mandatory gate usage, Todo writer behavior, and customer-master risk policy
  key_links:
    - from: runtime/gateway.py
      to: runtime/schema_preflight.py
      via: mandatory preflight collection for every candidate
      pattern: preflight_reports|SchemaPreflightRunner
    - from: runtime/gateway.py
      to: runtime/write_guard.py
      via: mandatory guard evaluation before execution
      pattern: guard_results|WriteGuard
    - from: runtime/todo_writer.py
      to: runtime/write_guard.py
      via: Todo confirmed-write enforcement after dedupe decisions
      pattern: evaluate\(|dedupe_decision
---

<objective>
Implement the runtime-side hardening for Phase 4 so the confirmed Todo write path is consistently gated and customer-master updates are explicitly split into low-risk fact candidates versus recommendation-only protected updates.

Purpose: Strengthen the actual safety contract of the write path without introducing a generic Base mutation framework.
Output: Updated runtime safety rules, Todo writer enforcement, semantic policy boundaries, and runtime-smoke regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-unified-safe-writes/04-CONTEXT.md
@.planning/phases/04-unified-safe-writes/04-RESEARCH.md
@.planning/phases/04-unified-safe-writes/04-VALIDATION.md
@.planning/phases/04-unified-safe-writes/04-01-PLAN.md
@runtime/gateway.py
@runtime/schema_preflight.py
@runtime/write_guard.py
@runtime/todo_writer.py
@runtime/semantic_registry.py
@tests/test_runtime_smoke.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regressions for mandatory gates and customer-master risk policy</name>
  <files>tests/test_runtime_smoke.py</files>
  <behavior>
    - Test 1: confirmed write candidates cannot bypass schema preflight or write guard.
    - Test 2: Todo create/update continues to return normalized blocked/allowed results for all dedupe branches.
    - Test 3: high-risk customer-master fields remain recommendation-only or blocked, while only an explicit low-risk fact allowlist can clear the runtime policy gate.
  </behavior>
  <action>Extend `tests/test_runtime_smoke.py` first to cover the runtime-side contract from D-08 through D-13. The new tests should pin the exact boundary: Todo create/update stays the primary confirmed execution surface, and customer-master policy widening must stay tiny, explicit, and fact-like. Do not invent tests for a generic Base writer; Phase 4 is about safety policy and narrowed readiness, not platform expansion.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_runtime_smoke -q</automated>
  </verify>
  <done>The failing runtime-smoke suite clearly describes the mandatory gate sequence and the customer-master low-risk/high-risk split that the implementation must satisfy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden runtime gate enforcement and narrow customer-master policy</name>
  <files>runtime/gateway.py, runtime/schema_preflight.py, runtime/write_guard.py, runtime/todo_writer.py, runtime/semantic_registry.py, tests/test_runtime_smoke.py</files>
  <behavior>
    - Test 1: Todo confirmed-write paths still always evaluate preflight and guard before create/update execution.
    - Test 2: customer-master candidates touching protected or judgment-heavy fields stay blocked or recommendation-only.
    - Test 3: only a tiny explicit allowlist of factual customer-master slots can become write-ready, and only under the existing live schema rules.
  </behavior>
  <action>Implement the narrow runtime hardening needed for Phase 4. In `runtime/schema_preflight.py` and `runtime/write_guard.py`, make the customer-master policy executable rather than documentary: guarded or interpretation-heavy slots remain blocked, while a tiny factual allowlist such as `last_contact_at` and `next_action_summary` may clear the gate when live schema checks pass. Keep `runtime/todo_writer.py` as the only fully hardened confirmed execution surface in this phase, and wire any policy changes through `runtime/gateway.py` so preflight and guard stay mandatory for every candidate. Use `runtime/semantic_registry.py` to keep the allowlist and protected-slot boundary explicit. Do not add a generic Base writer or widen scope beyond the small factual customer-master slice.</action>
  <verify>
    <automated>./.venv/bin/python -m unittest tests.test_runtime_smoke -q</automated>
  </verify>
  <done>The runtime enforces mandatory safety gates consistently, Todo execution remains trustworthy, and customer-master write readiness is explicitly limited to a tiny factual slice while high-risk fields stay guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| write candidate -> runtime safety gates | Untrusted candidate payloads must not bypass preflight or guard sequencing |
| customer-master candidate -> live Base policy | Customer-master updates must not gain write-ready status for judgment-heavy or protected fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | E | runtime/gateway.py | mitigate | Keep schema preflight and write guard mandatory for every candidate before any confirmed execution path runs |
| T-04-08 | T | runtime/schema_preflight.py | mitigate | Enforce live field-type, option, and write-policy checks so protected or drifted customer-master fields cannot become write-ready |
| T-04-09 | E | runtime/write_guard.py | mitigate | Add explicit object-level customer-master policy so only allowlisted factual slots can clear the final gate |
| T-04-10 | R | tests/test_runtime_smoke.py | mitigate | Cover blocked, allowed, and dedupe branches so later changes cannot quietly widen the confirmed-write surface |
</threat_model>

<verification>
- ./.venv/bin/python -m unittest tests.test_runtime_smoke -q
</verification>

<success_criteria>
- [ ] Preflight and guard are mandatory for every write-ready path.
- [ ] Todo create/update remains the only fully hardened confirmed execution path in Phase 4.
- [ ] Customer-master write readiness is explicitly limited to a tiny fact-like allowlist and protected fields remain recommendation-only or blocked.
</success_criteria>

<output>
After completion, create `.planning/phases/04-unified-safe-writes/04-03-SUMMARY.md`
</output>
