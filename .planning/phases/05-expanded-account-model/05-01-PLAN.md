---
phase: 05-expanded-account-model
plan: 01
type: execute
wave: 1
depends_on: ["04-03"]
files_modified:
  - evals/meeting_output_bridge.py
  - tests/test_meeting_output_bridge.py
autonomous: true
requirements: [WORK-02]
must_haves:
  truths:
    - User-facing context recovery can reference contracts, key people, and competitor context for a resolved customer.
    - Expanded reads remain tied to the resolved customer instead of becoming standalone table pulls.
    - Meeting and post-meeting output stays concise and recommendation-first.
  artifacts:
    - path: evals/meeting_output_bridge.py
      provides: targeted expanded-account read path and summary rendering
    - path: tests/test_meeting_output_bridge.py
      provides: regression coverage for expanded account context recovery
  key_links:
    - from: evals/meeting_output_bridge.py
      to: 合同清单 / 客户关键人地图 / 竞品交锋记录
      via: query_rows_by_customer_id
    - from: evals/meeting_output_bridge.py
      to: 竞品基础信息表
      via: query_rows_by_field_value on competitor ID
---

<objective>
Extend the existing customer-context recovery path so relevant scenes can use contracts, key people, and competitor structures without expanding into full-schema mirroring.

Purpose: complete WORK-02 by turning the existing table profiles into a live, targeted read path.
Output: expanded context-recovery summaries plus regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-expanded-account-model/05-CONTEXT.md
@evals/meeting_output_bridge.py
@tests/test_meeting_output_bridge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend context recovery with targeted expanded-account reads</name>
  <files>evals/meeting_output_bridge.py</files>
  <behavior>
    - Test 1: resolved customers can surface a concise 合同清单 summary
    - Test 2: resolved customers can surface a concise 客户关键人地图 summary
    - Test 3: 竞品交锋记录 can enrich from 竞品基础信息表 when competitor IDs are available
  </behavior>
  <action>Add a narrow expanded-account recovery step after the current 联系记录 and 行动计划 reads. Read 合同清单 and 客户关键人地图 by customer ID. Read 竞品交锋记录 by customer ID and enrich with 竞品基础信息表 only when the bridge rows expose a stable competitor ID, per D-04. Keep all output as concise summary lines and do not widen write scope, per D-03.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_meeting_output_bridge</automated>
  </verify>
  <done>Context recovery includes narrow summaries for contracts, key people, and competitor context when those sources exist.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the expanded-account behavior with regressions</name>
  <files>tests/test_meeting_output_bridge.py</files>
  <action>Add regression cases that prove expanded-account sources appear only through the resolved-customer path, and that competitor bridge rows still render useful context even when no dimension record is available.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_meeting_output_bridge</automated>
  </verify>
  <done>Regression coverage pins both enriched and bridge-only competitor paths, plus contract and key-people summaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| transcript/output → context recovery | user-facing meeting flow now sees more live account context |
| bridge rows → dimension lookup | competitor enrichment crosses from customer-specific bridge rows to shared competitor metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | I | evals/meeting_output_bridge.py | mitigate | keep summaries narrow and avoid dumping raw rows or full schemas |
| T-05-02 | T | evals/meeting_output_bridge.py | mitigate | require resolved customer ID for all expanded-account reads and competitor enrichment |
| T-05-03 | E | expanded account context | accept | Phase 5 is read-only and does not widen write permissions |
</threat_model>

<verification>
Run the meeting bridge regression suite and confirm the expanded-account model appears only as concise context summaries.
</verification>

<success_criteria>
- Contracts, key people, and competitor context can appear in context recovery for resolved customers.
- No new free-form browsing path or write path is introduced.
- WORK-02 is satisfied through executable regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/05-expanded-account-model/05-01-SUMMARY.md`
</output>
