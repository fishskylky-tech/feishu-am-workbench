---
phase: 06-validation-and-portability
plan: 01
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - tests/test_portability_contract.py
autonomous: true
requirements: [FOUND-03, VAL-01, VAL-02, PORT-01]
must_haves:
  truths:
    - Validation assets remain executable and cover the representative AM cases.
    - The repo's live-first and recommendation-first safety rules are still visible in its core guidance.
    - Core runtime and eval logic remain host-agnostic across Hermes/OpenClaw/Codex-style usage.
  artifacts:
    - path: tests/test_portability_contract.py
      provides: executable portability and rule-consistency contract
  key_links:
    - from: tests/test_portability_contract.py
      to: AGENTS.md / README.md / SKILL.md / .planning/PROJECT.md
      via: documentation contract assertions
    - from: tests/test_portability_contract.py
      to: runtime/ and evals/
      via: host-marker absence checks
---

<objective>
Close the milestone trust loop by making validation and host portability contractual through automated tests.

Purpose: satisfy FOUND-03, VAL-01, VAL-02, and PORT-01 without adding host-specific behavior to the domain core.
Output: portability-contract regressions and a passing validation suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-validation-and-portability/06-CONTEXT.md
@tests/test_validation_assets.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add executable portability and rule-consistency checks</name>
  <files>tests/test_portability_contract.py</files>
  <behavior>
    - Test 1: project-level docs declare Hermes/OpenClaw/Codex portability
    - Test 2: repo guidance still makes live-first and recommendation safety explicit
    - Test 3: runtime and eval core do not hard-code host-specific behavior
  </behavior>
  <action>Create a small dedicated regression suite that checks portability guidance in project docs, accepts current recommendation wording per D-03, and asserts that `runtime/` and core eval logic stay free of host markers per D-02.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_validation_assets tests.test_portability_contract</automated>
  </verify>
  <done>Validation and portability expectations are executable rather than implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the full Phase 5 and Phase 6 regression envelope</name>
  <files>tests/test_meeting_output_bridge.py, tests/test_runtime_smoke.py, tests/test_validation_assets.py, tests/test_portability_contract.py</files>
  <action>Run the combined regression suite covering expanded-account context recovery, runtime smoke coverage, validation assets, and host-portability contract checks so the milestone closes with one repeatable command.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_meeting_output_bridge tests.test_runtime_smoke tests.test_validation_assets tests.test_portability_contract</automated>
  </verify>
  <done>The combined validation suite passes and Phase 6 completion is backed by executable evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project docs → agent behavior | rule wording in docs influences how agent hosts operate |
| host guidance → core runtime | host-specific integration guidance must not leak into domain logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | project guidance | mitigate | pin live-first and recommendation safety markers in automated tests |
| T-06-02 | I | runtime and eval core | mitigate | assert host markers stay out of core account-operating code |
| T-06-03 | R | validation evidence | mitigate | run one combined regression suite that covers validation and portability |
</threat_model>

<verification>
Run the full Phase 5 and Phase 6 regression suite and keep the command reusable for milestone closeout.
</verification>

<success_criteria>
- FOUND-03, VAL-01, VAL-02, and PORT-01 are backed by passing automated checks.
- Core runtime and eval logic remain host-agnostic.
- Validation expectations are executable and repeatable from one command.
</success_criteria>

<output>
After completion, create `.planning/phases/06-validation-and-portability/06-01-SUMMARY.md`
</output>
