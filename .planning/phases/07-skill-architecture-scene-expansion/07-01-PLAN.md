---
phase: 07-skill-architecture-scene-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - ARCHITECTURE.md
  - references/scene-skill-architecture.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
must_haves:
  truths:
    - The repo defines a thin main skill that routes scenes and preserves one top-level interaction pattern instead of growing into a monolith.
    - First-wave scene skills are defined by AM workflows, with explicit priority and expert-agent collaboration rules.
    - Shared runtime foundation responsibilities remain stable and do not expand into default business-context assembly.
  artifacts:
    - path: SKILL.md
      provides: root-skill boundary and packaging direction aligned with the Phase 7 scene model
    - path: ARCHITECTURE.md
      provides: canonical four-layer contract across main skill, scene skills, expert agents, and runtime foundation
    - path: references/scene-skill-architecture.md
      provides: first-wave scene definitions, expert-agent return artifacts, and routing boundaries
  key_links:
    - from: SKILL.md
      to: references/scene-skill-architecture.md
      via: root-to-scene routing and progressive disclosure guidance
      pattern: scene skill|post-meeting-synthesis|customer-recent-status
    - from: ARCHITECTURE.md
      to: runtime/gateway.py
      via: shared foundation boundary
      pattern: thin foundation|customer resolution|schema preflight
---

<objective>
Lock the core Phase 7 architecture contract so the repository stops evolving as one expanding root skill and instead has an explicit thin-main-skill plus workflow-scene-skill plus expert-agent plus shared-foundation model.

Purpose: Establish the canonical layering and first-wave scene boundaries that later packaging or implementation work can execute against without reopening the architecture debate.
Output: Updated root skill and architecture docs plus a new scene-skill reference that defines workflow boundaries, first-wave priority, and expert-agent handoffs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-skill-architecture-scene-expansion/07-CONTEXT.md
@.planning/phases/07-skill-architecture-scene-expansion/07-RESEARCH.md
@.planning/quick/260415-nz8-feishu-am-workbench-skill-skill/260415-nz8-SUMMARY.md
@SKILL.md
@ARCHITECTURE.md
@docs/loading-strategy.md
@references/minimal-stable-core.md
@references/workbench-information-architecture.md
@runtime/gateway.py
@runtime/live_adapter.py
@runtime/schema_preflight.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical four-layer architecture and scene reference</name>
  <files>ARCHITECTURE.md, references/scene-skill-architecture.md</files>
  <action>Update `ARCHITECTURE.md` to make the Phase 7 contract explicit per D-01 through D-10 and D-21 through D-23: the main skill remains a thin orchestrator, scene skills are workflow-defined units, expert agents are scene-internal collaborators returning structured artifacts, and the shared foundation keeps ownership of resource probing, customer resolution, targeted reads, schema preflight, write guard, diagnostics, and normalized writer surfaces. Create `references/scene-skill-architecture.md` as the canonical first-wave scene document covering `post-meeting-synthesis`, `customer-recent-status`, `archive-refresh`, and `todo-capture-and-update`, their priority order per D-05 and D-06, the deferred scene list from D-07, and the structured expert outputs from D-08 through D-10. Do not define scenes by raw Feishu tables, and do not move business-context assembly into the foundation.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
root = Path('/Users/liaoky/.codex/skills/feishu-am-workbench')
checks = {
    'ARCHITECTURE.md': ['main skill', 'scene skills', 'expert agents', 'runtime foundation'],
    'references/scene-skill-architecture.md': ['post-meeting-synthesis', 'customer-recent-status', 'archive-refresh', 'todo-capture-and-update'],
}
for rel, needles in checks.items():
    text = (root / rel).read_text()
    missing = [needle for needle in needles if needle not in text]
    if missing:
        raise SystemExit(f'{rel} missing: {missing}')
print('ok')
PY</automated>
  </verify>
  <done>`ARCHITECTURE.md` and the new scene reference make the four-layer contract explicit, lock the first-wave scene boundaries, and define expert-agent collaboration without reopening table-driven packaging.</done>
</task>

<task type="auto">
  <name>Task 2: Re-scope the root skill as entry orchestration only</name>
  <files>SKILL.md</files>
  <action>Revise `SKILL.md` so the root skill clearly acts as the thin entry and orchestration layer per D-01, D-02, and D-03, while pointing deep scene logic toward scene-specific resources rather than continuing to expand one root instruction set. Keep the current progressive-disclosure pattern, but make the future packaging direction concrete: root skill handles scene detection, live-context decision, recommendation-confirm-write flow, and top-level interaction consistency; scene skills own workflow rules; expert agents are optional scene collaborators; the admin/bootstrap path is separate from daily scenes. Preserve all live-first and recommendation-first rules already locked in the root skill.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path('/Users/liaoky/.codex/skills/feishu-am-workbench/SKILL.md').read_text()
needles = [
    'thin entry and orchestration layer',
    'scene skills',
    'expert agents',
    'admin/bootstrap path',
]
missing = [needle for needle in needles if needle not in text]
if missing:
    raise SystemExit(f'SKILL.md missing: {missing}')
print('ok')
PY</automated>
  </verify>
  <done>The root `SKILL.md` no longer reads like the future home for all business logic, and it explicitly routes the repo toward scene-specific packaging while preserving the current safety and loading rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root skill -> scene skills | The entry layer must route and enforce top-level safety without absorbing scene-specific business reasoning |
| scene skills -> runtime foundation | Workflow logic chooses context and expert calls, while the foundation remains a reusable live-access and safety substrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | E | SKILL.md | mitigate | Explicitly define the root skill as orchestration-only so it cannot silently accumulate scene-specific authority or write logic |
| T-07-02 | T | references/scene-skill-architecture.md | mitigate | Define scenes by AM workflows and lock the first wave plus deferred list so later packaging does not drift back to table-driven decomposition |
| T-07-03 | I | ARCHITECTURE.md | mitigate | Keep the shared foundation thin and reusable, and document that it does not assemble default business-context bundles for every scene |
</threat_model>

<verification>
- Validate the four-layer contract appears in `ARCHITECTURE.md`.
- Validate the first-wave scenes and deferred scenes appear in `references/scene-skill-architecture.md`.
- Validate `SKILL.md` states the root skill remains a thin entry and orchestration layer.
</verification>

<success_criteria>
- [ ] The repo has a canonical document describing main skill, scene skills, expert agents, and shared runtime foundation as distinct layers.
- [ ] The first-wave scene list and its priority order are explicit and workflow-driven.
- [ ] Expert agents are documented as scene-internal collaborators returning structured artifacts.
- [ ] The root skill boundary remains thin and does not reclaim deep scene logic.
</success_criteria>

<output>
After completion, create `.planning/phases/07-skill-architecture-scene-expansion/07-01-SUMMARY.md`
</output>
