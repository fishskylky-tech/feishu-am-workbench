---
phase: 07-skill-architecture-scene-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONFIG-MODEL.md
  - references/workspace-bootstrap.md
  - references/cache-governance.md
autonomous: true
requirements:
  - ARCH-03
  - ARCH-04
must_haves:
  truths:
    - Bootstrap and compatibility work is defined as a separate admin path rather than part of daily scene execution.
    - The architecture defines minimum bootstrap outputs, controlled remote-init boundaries, and strong-confirmation expectations.
    - Schema, manifest/index, and semantic/ontology caches are documented as subordinate acceleration layers with explicit refresh lifecycle and live-confirm rules.
  artifacts:
    - path: CONFIG-MODEL.md
      provides: workspace-config and admin-path boundary aligned with bootstrap responsibilities
    - path: references/workspace-bootstrap.md
      provides: bootstrap deliverables, compatibility checks, and guarded remote initialization contract
    - path: references/cache-governance.md
      provides: three cache classes, trust hierarchy, refresh lifecycle, and write-time live-confirm rule
  key_links:
    - from: references/workspace-bootstrap.md
      to: CONFIG-MODEL.md
      via: workspace config generation and compatibility reporting
      pattern: workspace compatibility|local config|lark-cli
    - from: references/cache-governance.md
      to: references/live-schema-preflight.md
      via: cache remains subordinate to live confirmation before writes
      pattern: live confirm|preflight|cache refresh
---

<objective>
Define the Phase 7 bootstrap/admin and cache contract so setup, compatibility checks, and acceleration artifacts become explicit extension surfaces without weakening daily scene safety.

Purpose: Prevent bootstrap and cache behavior from leaking into daily scenes or replacing live truth as the authority for critical reads and writes.
Output: Updated config model plus new bootstrap and cache governance references that lock deliverables, trust hierarchy, and refresh lifecycle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-skill-architecture-scene-expansion/07-CONTEXT.md
@.planning/phases/07-skill-architecture-scene-expansion/07-RESEARCH.md
@.planning/quick/260415-nz8-feishu-am-workbench-skill-skill/260415-nz8-SUMMARY.md
@CONFIG-MODEL.md
@references/minimal-stable-core.md
@references/feishu-runtime-sources.md
@references/base-integration-model.md
@references/live-schema-preflight.md
@config/template.yaml
@config/example.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the separate workspace bootstrap and admin contract</name>
  <files>CONFIG-MODEL.md, references/workspace-bootstrap.md</files>
  <action>Update `CONFIG-MODEL.md` and create `references/workspace-bootstrap.md` so the Phase 7 admin path is explicit per D-11 through D-14: bootstrap, install, compatibility, environment diagnosis, local config generation or update guidance, cache initialization or refresh reporting, drift and risk reporting, and `lark-cli` version dependency checks live in a separate admin/bootstrap path rather than in the daily main-skill flow. Document the allowed controlled remote initialization surface as admin-only and strong-confirmation-gated, and keep daily scene safety rules unchanged. Tie bootstrap responsibilities back to workspace config, not to checked-in private resource identifiers.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
root = Path('/Users/liaoky/.codex/skills/feishu-am-workbench')
checks = {
    'CONFIG-MODEL.md': ['workspace config', 'bootstrap', 'guardrails'],
    'references/workspace-bootstrap.md': ['workspace compatibility', 'lark-cli', 'strong confirmation'],
}
for rel, needles in checks.items():
    text = (root / rel).read_text()
    missing = [needle for needle in needles if needle not in text]
    if missing:
        raise SystemExit(f'{rel} missing: {missing}')
print('ok')
PY</automated>
  </verify>
  <done>The repo has an explicit admin/bootstrap contract with minimum deliverables, workspace-config linkage, and strong-confirmation limits for any controlled remote initialization.</done>
</task>

<task type="auto">
  <name>Task 2: Lock cache classes, trust hierarchy, and refresh lifecycle</name>
  <files>references/cache-governance.md</files>
  <action>Create `references/cache-governance.md` to formalize the three cache classes from D-15 through D-20: workspace schema cache, workspace manifest/index cache, and semantic/ontology cache. For each class, define purpose, authoritative source, allowed uses, refresh lifecycle, drift handling, and when live confirmation is mandatory. State explicitly that cache can support routing and early reasoning, but critical reads confirm live state when needed and every write path still requires live confirmation before mutation. Keep artifact file formats and exact refresh commands implementation-discretionary unless needed to express the trust hierarchy.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path('/Users/liaoky/.codex/skills/feishu-am-workbench/references/cache-governance.md').read_text()
needles = [
    'workspace schema cache',
    'workspace manifest/index cache',
    'semantic/ontology cache',
    'live confirmation',
    'refresh lifecycle',
]
missing = [needle for needle in needles if needle not in text]
if missing:
    raise SystemExit(f'cache-governance missing: {missing}')
print('ok')
PY</automated>
  </verify>
  <done>The repo documents all three cache classes, their subordinate trust hierarchy, and the rule that cache never outranks live truth for critical reads or writes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin/bootstrap path -> live workspace resources | Setup and compatibility flows may inspect or initialize resources and therefore need stricter confirmation boundaries than normal scene execution |
| cache artifacts -> runtime reads and writes | Cached schema or routing hints can accelerate decisions but must not replace live confirmation for safety-critical operations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | E | references/workspace-bootstrap.md | mitigate | Constrain controlled remote initialization to the admin path and require strong confirmation before any remote creation or repair |
| T-07-05 | T | references/cache-governance.md | mitigate | Document the trust hierarchy so schema, manifest, and semantic caches are acceleration layers rather than silent runtime truth sources |
| T-07-06 | I | CONFIG-MODEL.md | mitigate | Keep workspace config as the environment boundary and forbid checked-in docs or cache artifacts from acting as live private resource truth |
</threat_model>

<verification>
- Validate bootstrap deliverables and strong-confirmation wording appear in the new bootstrap reference.
- Validate all three cache classes and live-confirm rules appear in the cache governance reference.
- Validate `CONFIG-MODEL.md` still treats workspace config as the environment boundary.
</verification>

<success_criteria>
- [ ] The repo has a separate admin/bootstrap contract rather than mixed setup logic in the daily main-skill path.
- [ ] Bootstrap minimum outputs and controlled remote-init boundaries are explicit.
- [ ] The repo documents schema, manifest/index, and semantic/ontology cache as separate classes.
- [ ] Cache refresh and live-confirm rules are explicit enough to prevent cache from becoming silent runtime truth.
</success_criteria>

<output>
After completion, create `.planning/phases/07-skill-architecture-scene-expansion/07-02-SUMMARY.md`
</output>
