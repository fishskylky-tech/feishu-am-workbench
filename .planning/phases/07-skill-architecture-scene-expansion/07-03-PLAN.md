---
phase: 07-skill-architecture-scene-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - README.md
  - docs/loading-strategy.md
  - references/INDEX.md
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-03
  - ARCH-04
must_haves:
  truths:
    - A future executor can discover the multi-skill packaging model from the repo entry docs without reverse-engineering Phase 7 decisions.
    - Loading guidance explains how the root skill, future scene skills, and admin/bootstrap path preserve progressive disclosure.
    - Reference indexing and top-level docs point to the new scene, bootstrap, and cache contracts as canonical guidance.
  artifacts:
    - path: README.md
      provides: top-level repository explanation of the new packaging direction and current-vs-target boundary
    - path: docs/loading-strategy.md
      provides: progressive disclosure strategy for root skill, future scene skills, and admin/bootstrap packaging
    - path: references/INDEX.md
      provides: discoverable index entries for the new scene, bootstrap, and cache contract references
  key_links:
    - from: README.md
      to: docs/loading-strategy.md
      via: packaging direction and progressive disclosure explanation
      pattern: scene skills|admin/bootstrap|loading strategy
    - from: references/INDEX.md
      to: references/scene-skill-architecture.md
      via: reference discoverability for future phase execution
      pattern: scene-skill-architecture|workspace-bootstrap|cache-governance
---

<objective>
Integrate the Phase 7 architecture contract back into the repo entry docs so the new packaging model is discoverable, loadable, and ready for later execution without re-reading the whole planning history.

Purpose: Turn the architecture decision into a stable repo operating surface rather than leaving it isolated in plan artifacts.
Output: Updated README, loading strategy, and reference index aligned with the new main-skill, scene-skill, bootstrap, and cache contract docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-skill-architecture-scene-expansion/07-CONTEXT.md
@.planning/phases/07-skill-architecture-scene-expansion/07-RESEARCH.md
@.planning/phases/07-skill-architecture-scene-expansion/07-01-PLAN.md
@.planning/phases/07-skill-architecture-scene-expansion/07-02-PLAN.md
@README.md
@docs/loading-strategy.md
@references/INDEX.md
@SKILL.md
@ARCHITECTURE.md
@references/scene-skill-architecture.md
@references/workspace-bootstrap.md
@references/cache-governance.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the packaging direction into top-level docs</name>
  <files>README.md, docs/loading-strategy.md</files>
  <action>Update `README.md` and `docs/loading-strategy.md` so they reflect the locked Phase 7 packaging direction rather than the older single-root-skill assumption. `README.md` should explain the current state versus target state: root skill still exists today, but the future architecture is a thin entry layer plus scene skills plus expert agents plus admin/bootstrap path over one shared runtime foundation. `docs/loading-strategy.md` should explain how progressive disclosure carries into future scene-skill folders and how the admin/bootstrap path stays separate from daily scene loading. Preserve the repo's docs-first style and avoid claiming that scene folders already exist if they are only planned in this phase.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
root = Path('/Users/liaoky/.codex/skills/feishu-am-workbench')
checks = {
    'README.md': ['scene skills', 'admin/bootstrap', 'runtime foundation'],
    'docs/loading-strategy.md': ['root skill', 'scene skills', 'admin/bootstrap'],
}
for rel, needles in checks.items():
    text = (root / rel).read_text()
    missing = [needle for needle in needles if needle not in text]
    if missing:
        raise SystemExit(f'{rel} missing: {missing}')
print('ok')
PY</automated>
  </verify>
  <done>Top-level docs explain the current-to-target packaging move clearly enough that future contributors do not need to infer Phase 7 decisions from planning artifacts alone.</done>
</task>

<task type="auto">
  <name>Task 2: Index the new architecture references for future loading and execution</name>
  <files>references/INDEX.md</files>
  <action>Update `references/INDEX.md` to include the new `scene-skill-architecture.md`, `workspace-bootstrap.md`, and `cache-governance.md` references with loading guidance that matches the Phase 7 contract. Group them so future agents know when to load scene-packaging guidance, bootstrap/admin guidance, and cache-governance guidance. Keep the index aligned with the progressive-disclosure strategy and make sure the descriptions reinforce that cache remains subordinate to live truth and bootstrap remains separate from daily scene execution.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path('/Users/liaoky/.codex/skills/feishu-am-workbench/references/INDEX.md').read_text()
needles = ['scene-skill-architecture.md', 'workspace-bootstrap.md', 'cache-governance.md']
missing = [needle for needle in needles if needle not in text]
if missing:
    raise SystemExit(f'INDEX missing: {missing}')
print('ok')
PY</automated>
  </verify>
  <done>The reference index exposes the new Phase 7 architecture documents as first-class loadable guidance for later execution phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| top-level docs -> future executors | Entry docs shape how future work is interpreted; stale or ambiguous guidance can reintroduce monolithic skill growth or unsafe bootstrap assumptions |
| reference index -> loading behavior | Incorrect indexing can cause future agents to miss the new architecture constraints or load the wrong guidance at the wrong time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | T | README.md | mitigate | Distinguish clearly between current repo state and target packaging direction so later work does not claim unimplemented scene folders already exist |
| T-07-08 | I | docs/loading-strategy.md | mitigate | Update progressive-disclosure guidance to cover root, scene, and admin/bootstrap loading without weakening live-first or recommendation-first rules |
| T-07-09 | D | references/INDEX.md | mitigate | Index the new architecture references explicitly so future agents can load the right documents instead of overloading the root skill or missing bootstrap/cache constraints |
</threat_model>

<verification>
- Validate README mentions scene skills, admin/bootstrap, and shared runtime foundation.
- Validate loading-strategy explains root-vs-scene-vs-admin loading.
- Validate references/INDEX includes the three new Phase 7 references.
</verification>

<success_criteria>
- [ ] The repo entry docs explain the new architecture direction without overstating current implementation.
- [ ] Progressive disclosure guidance covers the future multi-skill packaging model.
- [ ] The reference index exposes the new Phase 7 architecture docs as loadable references.
- [ ] Future phases can discover Phase 7 contracts from canonical docs instead of relying on quick-task history.
</success_criteria>

<output>
After completion, create `.planning/phases/07-skill-architecture-scene-expansion/07-03-SUMMARY.md`
</output>
