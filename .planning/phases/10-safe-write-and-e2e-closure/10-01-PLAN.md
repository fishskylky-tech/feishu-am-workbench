---
phase: 10-safe-write-and-e2e-closure
plan: 01
type: execute
wave: 1
depends_on: ["08-01", "09-01"]
files_modified:
  - evals/meeting_output_bridge.py
  - tests/test_meeting_output_bridge.py
  - tests/test_validation_assets.py
  - .planning/phases/04-unified-safe-writes/04-VALIDATION.md
  - .planning/phases/04-unified-safe-writes/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MEET-03, WRITE-01, WRITE-02, WRITE-03, VAL-01, VAL-02]
must_haves:
  truths:
    - Meeting Todo candidate generation no longer depends on one hard-coded eval name.
    - Phase 4 validation and verification are milestone-grade and Nyquist-compliant.
    - The repo has one auditable E2E proof path for the meeting write loop.
---

<objective>
Finish the milestone by closing the remaining safe-write and E2E proof gaps.

Purpose: satisfy Phase 10 by generalizing candidate generation, validating the write loop, and making Phase 4 fully auditable.
Output: generalized MEET-03 implementation, passing E2E regressions, and closed Phase 4 validation/verification artifacts.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove eval-specific candidate generation gate</name>
  <files>evals/meeting_output_bridge.py, tests/test_meeting_output_bridge.py</files>
  <action>Add a failing regression for non-unilever resolved customers, then remove the explicit eval-name gate so recommendation-mode Todo candidates remain available across resolved meeting cases.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_meeting_output_bridge -q</automated>
  </verify>
  <done>MEET-03 is no longer limited to one eval fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Add auditable E2E proof and close Phase 4 validation</name>
  <files>tests/test_meeting_output_bridge.py, tests/test_validation_assets.py, .planning/phases/04-unified-safe-writes/04-VALIDATION.md, .planning/phases/04-unified-safe-writes/04-VERIFICATION.md</files>
  <action>Add one executable meeting write loop proof path, then mark Phase 4 validation/verification complete while keeping manual/live checks explicit.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_runtime_smoke tests.test_meeting_output_bridge tests.test_eval_runner tests.test_validation_assets -q</automated>
  </verify>
  <done>Safe-write closure and E2E proof are both milestone-auditable.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/10-safe-write-and-e2e-closure/10-01-SUMMARY.md` and `.planning/phases/10-safe-write-and-e2e-closure/10-VERIFICATION.md`
</output>