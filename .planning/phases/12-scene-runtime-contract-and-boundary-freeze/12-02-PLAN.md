---
phase: 12-scene-runtime-contract-and-boundary-freeze
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - references/scene-runtime-contract.md
  - references/scene-skill-architecture.md
  - ARCHITECTURE.md
  - runtime/README.md
  - tests/test_validation_assets.py
autonomous: true
requirements: [SCENE-03, SAFE-01]
must_haves:
  truths:
    - First-wave scene boundaries are frozen by workflow, priority group, and deferred-scene rules before later implementation phases start.
    - Scene docs and validation state that gateway, schema preflight, write guard, and writer are non-bypassable shared boundaries.
    - Recommendation-first and live-first rules remain explicit at the scene runtime layer instead of being implied by older meeting-only paths.
  artifacts:
    - path: references/scene-runtime-contract.md
      provides: Canonical scene execution contract and non-bypass rules.
      contains: fallback reason
    - path: references/scene-skill-architecture.md
      provides: Frozen first-wave scenes and deferred-scene rules aligned with runtime execution.
      contains: post-meeting-synthesis
    - path: tests/test_validation_assets.py
      provides: Regression checks that keep the new scene contract and boundary rules from drifting.
      contains: scene-runtime-contract
  key_links:
    - from: references/scene-runtime-contract.md
      to: runtime/README.md
      via: runtime boundary wording
      pattern: gateway|schema preflight|write guard|writer
    - from: references/scene-skill-architecture.md
      to: ARCHITECTURE.md
      via: first-wave scene boundary freeze
      pattern: customer-recent-status|archive-refresh|todo-capture-and-update
    - from: tests/test_validation_assets.py
      to: references/scene-runtime-contract.md
      via: documentation consistency regression
      pattern: live-first|recommendation-first
---

<objective>
Freeze first-wave scene boundaries and lock the non-bypass rules that later scene implementations must respect.

Purpose: satisfy SCENE-03 and SAFE-01 before Phase 13 and later scenes build on a drifting contract.
Output: canonical runtime boundary reference, aligned architecture/runtime docs, and validation checks that keep the contract stable.
</objective>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-scene-runtime-contract-and-boundary-freeze/12-RESEARCH.md
@.planning/phases/07-skill-architecture-scene-expansion/07-01-SUMMARY.md
@references/scene-skill-architecture.md
@ARCHITECTURE.md
@runtime/README.md
@tests/test_validation_assets.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical scene runtime boundary reference</name>
  <files>references/scene-runtime-contract.md, references/scene-skill-architecture.md, ARCHITECTURE.md, runtime/README.md</files>
  <action>Create `references/scene-runtime-contract.md` as the canonical execution contract for scenes: required input envelope, required structured result fields, fallback-reason visibility, write ceiling semantics, and the explicit rule that gateway, schema preflight, write guard, and writer remain the only shared safety path. Then align `references/scene-skill-architecture.md`, `ARCHITECTURE.md`, and `runtime/README.md` so the first-wave scenes and deferred scenes are frozen in the same wording as runtime constraints, not just packaging guidance.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_validation_assets -q</automated>
  </verify>
  <done>The scene runtime contract and first-wave boundary freeze are documented in one consistent architecture/reference surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression checks for scene-boundary and safety drift</name>
  <files>tests/test_validation_assets.py, references/scene-runtime-contract.md, runtime/README.md</files>
  <behavior>
    - Test 1: validation assets fail if the canonical scene runtime contract reference disappears or drops live-first and recommendation-first wording.
    - Test 2: validation assets fail if runtime docs stop naming gateway, schema preflight, write guard, and writer as non-bypassable boundaries.
    - Test 3: validation assets fail if first-wave scene names drift away from the Phase 7 locked list.
  </behavior>
  <action>Add validation assertions that lock the new contract wording and first-wave scene list. Keep the checks narrow and textual so they guard planning/architecture drift without forcing implementation details that belong to later phases.</action>
  <verify>
    <automated>source .venv/bin/activate && python -m unittest tests.test_validation_assets -q</automated>
  </verify>
  <done>Phase 12 has executable regression checks that catch scene-boundary drift and non-bypass safety regressions early.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| architecture/reference docs -> later scene implementations | Later phases will treat these documents as the contract for allowed scene behavior |
| validation tests -> planning and release truth | Validation assets must detect drift before later scene work normalizes an unsafe boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04 | T | references/scene-runtime-contract.md | mitigate | Make the non-bypass rule explicit and align all runtime boundary docs to the same source contract |
| T-12-05 | R | references/scene-skill-architecture.md | mitigate | Freeze first-wave and deferred scene lists so later phases cannot silently widen scope |
| T-12-06 | T | tests/test_validation_assets.py | mitigate | Add regression assertions for live-first, recommendation-first, and gateway/preflight/guard/writer wording |
</threat_model>

<verification>
Run validation-asset regression checks after updating the scene contract reference and aligned docs, then confirm the first-wave scene list and non-bypass rules are enforced by tests.
</verification>

<success_criteria>
1. First-wave scene boundaries are frozen in docs and references.
2. The non-bypass rule for gateway, preflight, guard, and writer is explicit and test-guarded.
3. Live-first and recommendation-first wording remains part of the scene runtime contract.
</success_criteria>

<output>
After completion, create `.planning/phases/12-scene-runtime-contract-and-boundary-freeze/12-02-SUMMARY.md`
</output>
