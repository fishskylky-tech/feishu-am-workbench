---
phase: "21"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_scene_runtime.py
  - tests/test_archive_refresh_scene.py
autonomous: true
requirements:
  - VAL-05
must_haves:
  truths:
    - "post-meeting-synthesis scene handles happy-path, limited-context, unresolved-customer, and blocked-write correctly"
    - "customer-recent-status scene handles happy-path, limited-context, unresolved-customer, and blocked-write correctly"
    - "archive-refresh scene handles happy-path, limited-context, unresolved-customer, and blocked-write correctly"
  artifacts:
    - path: "tests/test_scene_runtime.py"
      provides: "post-meeting-synthesis and customer-recent-status regression coverage for 4 case types"
    - path: "tests/test_archive_refresh_scene.py"
      provides: "archive-refresh regression coverage for 4 case types"
---

<objective>
Add scene-contract regression coverage for post-meeting-synthesis, customer-recent-status, and archive-refresh scenes across all 4 case types (happy-path, limited-context, unresolved-customer, blocked-write) per VAL-05 and D-01 to D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@runtime/scene_runtime.py
@runtime/scene_registry.py
@runtime/models.py
@tests/test_scene_runtime.py
@tests/test_archive_refresh_scene.py
</context>

<interfaces>
<!-- SceneRequest/SceneResult contract from runtime/scene_runtime.py (lines 557-582) -->
@dataclass
class SceneRequest:
    scene_name: str
    repo_root: Path
    customer_query: str
    inputs: dict[str, Any] = field(default_factory=dict)
    options: dict[str, Any] = field(default_factory=dict)

@dataclass
class SceneResult:
    scene_name: str
    resource_status: str  # "live" | "partial" | "unavailable"
    customer_status: str  # "resolved" | "ambiguous" | "not_found"
    context_status: str   # "complete" | "partial" | "minimal"
    write_ceiling: str   # "normal" | "recommendation-only"
    used_sources: list[str] = field(default_factory=list)
    facts: list[str] = field(default_factory=list)
    judgments: list[str] = field(default_factory=list)
    open_questions: list[str] = field(default_factory=list)
    recommendations: list[str] = field(default_factory=list)
    fallback_category: SceneFallbackCategory = "none"
    fallback_reason: str | None = None
    fallback_message: str | None = None
    output_text: str = ""
    payload: dict[str, Any] = field(default_factory=dict)

<!-- Dispatch entry from runtime/scene_registry.py -->
def dispatch_scene(request: SceneRequest, registry: SceneRegistry | None = None) -> SceneResult:
    active_registry = registry or build_default_scene_registry()
    return active_registry.dispatch(request)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add happy-path and limited-context regression for post-meeting-synthesis</name>
  <files>tests/test_scene_runtime.py</files>
  <read_first>
    - tests/test_scene_runtime.py (existing post-meeting tests, lines 1-200)
    - runtime/scene_runtime.py (run_post_meeting_scene function signature)
    - runtime/scene_registry.py (scene registration)
  </read_first>
  <action>
    Add TestPostMeetingRegression class to tests/test_scene_runtime.py with 4 test methods:

    1. test_post_meeting_happy_path_dispatch_and_result_shape: Create SceneRequest with scene_name="post-meeting-synthesis", customer_query="测试客户", inputs={} (or minimal transcript). Call dispatch_scene(). Assert:
       - result.scene_name == "post-meeting-synthesis"
       - result.resource_status in ("live", "partial")
       - result.customer_status == "resolved"
       - result.context_status in ("complete", "partial")
       - result.write_ceiling in ("normal", "recommendation-only")
       - result.output_text is not empty
       - result.fallback_category in ("none", "context", "permission")

    2. test_post_meeting_limited_context_fallback_visible: Create SceneRequest with inputs that simulate partial evidence (e.g., empty transcript, some sources available). Call dispatch_scene(). Assert:
       - result.context_status in ("partial", "minimal")
       - result.fallback_category in ("context", "none")
       - result.fallback_message is not None or result.output_text is not empty (scene still produces output)

    3. test_post_meeting_unresolved_customer_handled: Create SceneRequest with customer_query that cannot be resolved (use invalid customer). Call dispatch_scene(). Assert:
       - result.customer_status in ("not_found", "ambiguous")
       - result.fallback_category == "customer"
       - result.write_ceiling == "recommendation-only"

    4. test_post_meeting_blocked_write_ceiling: Create SceneRequest with options={"confirmed_write": True} but simulate blocked condition. Assert:
       - result.write_ceiling == "recommendation-only"
       - result.fallback_category in ("safety", "permission", "validation")
       - No actual write executed (check result.recommendations exist, not result.payload with write indicators)

    Use unittest assertions (self.assert*). Do NOT use pytest fixtures or pytest-specific syntax.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_scene_runtime.TestPostMeetingRegression -v 2>&1 | head -50</automated>
  </verify>
  <done>
    TestPostMeetingRegression class exists in tests/test_scene_runtime.py with 4 test methods covering happy-path, limited-context, unresolved-customer, and blocked-write cases for post-meeting-synthesis scene
  </done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for customer-recent-status 4 case types</name>
  <files>tests/test_scene_runtime.py</files>
  <read_first>
    - tests/test_scene_runtime.py (existing tests, to find insertion point)
    - runtime/scene_runtime.py (run_customer_recent_status_scene function)
  </read_first>
  <action>
    Add TestCustomerRecentStatusRegression class to tests/test_scene_runtime.py (append after TestPostMeetingRegression) with 4 test methods:

    1. test_customer_recent_status_happy_path_dispatch_and_result_shape: Create SceneRequest with scene_name="customer-recent-status", customer_query="测试客户", inputs={}. Call dispatch_scene(). Assert:
       - result.scene_name == "customer-recent-status"
       - result.resource_status in ("live", "partial")
       - result.customer_status == "resolved"
       - result.context_status in ("complete", "partial")
       - result.write_ceiling in ("normal", "recommendation-only")
       - result.payload contains "lens_results" or result.output_text is not empty

    2. test_customer_recent_status_limited_context_fallback_visible: Create SceneRequest with partial evidence inputs. Assert:
       - result.context_status in ("partial", "minimal")
       - result.fallback_message is not None or result.output_text is not empty

    3. test_customer_recent_status_unresolved_customer: Create SceneRequest with invalid customer_query. Assert:
       - result.customer_status in ("not_found", "ambiguous")
       - result.fallback_category == "customer"

    4. test_customer_recent_status_blocked_write: Create SceneRequest with blocked write condition. Assert:
       - result.write_ceiling == "recommendation-only"
       - result.fallback_category in ("safety", "permission", "validation")

    Use unittest assertions. Add class after TestPostMeetingRegression.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_scene_runtime.TestCustomerRecentStatusRegression -v 2>&1 | head -50</automated>
  </verify>
  <done>
    TestCustomerRecentStatusRegression class exists in tests/test_scene_runtime.py with 4 test methods covering happy-path, limited-context, unresolved-customer, and blocked-write cases for customer-recent-status scene
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for archive-refresh 4 case types</name>
  <files>tests/test_archive_refresh_scene.py</files>
  <read_first>
    - tests/test_archive_refresh_scene.py (existing tests, to understand structure)
    - runtime/scene_runtime.py (run_archive_refresh_scene function, _derive_archive_refresh_lenses, _render_archive_refresh_output)
  </read_first>
  <action>
    Add TestArchiveRefreshRegression class to tests/test_archive_refresh_scene.py with 4 test methods:

    1. test_archive_refresh_happy_path_dispatch_and_result_shape: Create SceneRequest with scene_name="archive-refresh", customer_query="测试客户", inputs={}. Call dispatch_scene(). Assert:
       - result.scene_name == "archive-refresh"
       - result.resource_status in ("live", "partial")
       - result.customer_status == "resolved"
       - result.context_status in ("complete", "partial")
       - result.write_ceiling in ("normal", "recommendation-only")
       - result.payload contains five-dimension keys or result.output_text is not empty

    2. test_archive_refresh_limited_context_fallback_visible: Create SceneRequest with partial evidence. Assert:
       - result.context_status in ("partial", "minimal")
       - result.fallback_message is not None or result.output_text is not empty

    3. test_archive_refresh_unresolved_customer: Create SceneRequest with invalid customer. Assert:
       - result.customer_status in ("not_found", "ambiguous")
       - result.fallback_category == "customer"

    4. test_archive_refresh_blocked_write: Create SceneRequest with blocked write. Assert:
       - result.write_ceiling == "recommendation-only"

    Use unittest assertions.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_archive_refresh_scene.TestArchiveRefreshRegression -v 2>&1 | head -50</automated>
  </verify>
  <done>
    TestArchiveRefreshRegression class exists in tests/test_archive_refresh_scene.py with 4 test methods covering happy-path, limited-context, unresolved-customer, and blocked-write cases for archive-refresh scene
  </done>
</task>

</tasks>

<verification>
Run: python3 -m unittest tests.test_scene_runtime.TestPostMeetingRegression tests.test_scene_runtime.TestCustomerRecentStatusRegression tests.test_archive_refresh_scene.TestArchiveRefreshRegression -v
All 12 tests pass (3 scenes × 4 case types).
</verification>

<success_criteria>
- TestPostMeetingRegression: 4 tests pass
- TestCustomerRecentStatusRegression: 4 tests pass
- TestArchiveRefreshRegression: 4 tests pass
- All tests use SceneRequest -> dispatch_scene() -> SceneResult pattern
- Each test verifies scene_name, resource_status, customer_status, context_status, write_ceiling fields
- Fallback cases verify fallback_category is set appropriately
</success_criteria>

<output>
After completion, create .planning/phases/21-validation-and-milestone-closure/21-01-SUMMARY.md
</output>
