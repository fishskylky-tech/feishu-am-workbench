---
phase: "21"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cohort_scan.py
  - tests/test_meeting_prep_scene.py
  - tests/test_proposal_scene.py
autonomous: true
requirements:
  - VAL-05
must_haves:
  truths:
    - "cohort-scan scene handles happy-path, limited-context, unresolved-customer, and blocked-write correctly"
    - "meeting-prep scene handles happy-path, limited-context, unresolved-customer, and blocked-write correctly"
    - "proposal scene handles happy-path, limited-context, unresolved-customer, and blocked-write correctly"
  artifacts:
    - path: "tests/test_cohort_scan.py"
      provides: "cohort-scan regression coverage for 4 case types"
    - path: "tests/test_meeting_prep_scene.py"
      provides: "meeting-prep regression coverage for 4 case types"
    - path: "tests/test_proposal_scene.py"
      provides: "proposal regression coverage for 4 case types"
---

<objective>
Add scene-contract regression coverage for cohort-scan, meeting-prep, and proposal scenes across all 4 case types (happy-path, limited-context, unresolved-customer, blocked-write) per VAL-05 and D-01 to D-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@runtime/scene_runtime.py
@runtime/scene_registry.py
@runtime/models.py
@tests/test_cohort_scan.py
@tests/test_meeting_prep_scene.py
@tests/test_proposal_scene.py
</context>

<interfaces>
<!-- SceneRequest/SceneResult contract from runtime/scene_runtime.py (lines 557-582) -->
@dataclass
class SceneRequest:
    scene_name: str
    repo_root: Path
    customer_query: str
    inputs: dict[str, Any] = field(default_factory=dict)
    options: dict[str, Any] = field(default_factory=dict)

@dataclass
class SceneResult:
    scene_name: str
    resource_status: str
    customer_status: str
    context_status: str
    write_ceiling: str
    used_sources: list[str] = field(default_factory=list)
    facts: list[str] = field(default_factory=list)
    judgments: list[str] = field(default_factory=list)
    open_questions: list[str] = field(default_factory=list)
    recommendations: list[str] = field(default_factory=list)
    fallback_category: SceneFallbackCategory = "none"
    fallback_reason: str | None = None
    fallback_message: str | None = None
    output_text: str = ""
    payload: dict[str, Any] = field(default_factory=dict)

<!-- Dispatch entry from runtime/scene_registry.py -->
def dispatch_scene(request: SceneRequest, registry: SceneRegistry | None = None) -> SceneResult:
    active_registry = registry or build_default_scene_registry()
    return active_registry.dispatch(request)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for cohort-scan 4 case types</name>
  <files>tests/test_cohort_scan.py</files>
  <read_first>
    - tests/test_cohort_scan.py (existing tests to understand structure)
    - runtime/scene_runtime.py (run_cohort_scan_scene function)
  </read_first>
  <action>
    Add TestCohortScanRegression class to tests/test_cohort_scan.py with 4 test methods:

    1. test_cohort_scan_happy_path_dispatch_and_result_shape: Create SceneRequest with scene_name="cohort-scan", customer_query="活跃客户", inputs={}. Call dispatch_scene(). Assert:
       - result.scene_name == "cohort-scan"
       - result.resource_status in ("live", "partial")
       - result.context_status in ("complete", "partial")
       - result.write_ceiling in ("normal", "recommendation-only")
       - result.payload is not None and len(result.payload) > 0

    2. test_cohort_scan_limited_context_fallback_visible: Create SceneRequest with partial evidence. Assert:
       - result.context_status in ("partial", "minimal")
       - result.fallback_category in ("context", "none")
       - result.output_text is not None or result.recommendations is not None

    3. test_cohort_scan_unresolved_or_empty_cohort: Create SceneRequest with invalid/unresolvable cohort query. Assert:
       - result.fallback_category in ("customer", "context")
       - result.write_ceiling == "recommendation-only" or len(result.output_text) > 0

    4. test_cohort_scan_blocked_write: Create SceneRequest with blocked write condition. Assert:
       - result.write_ceiling == "recommendation-only"
       - result.fallback_category in ("customer", "safety", "permission")
       - result.recommendations is not None and len(result.recommendations) > 0
       - result.payload is not None and "write_candidates" not in result.payload

    Use unittest assertions.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_cohort_scan.TestCohortScanRegression -v 2>&1 | head -50</automated>
  </verify>
  <done>
    TestCohortScanRegression class exists in tests/test_cohort_scan.py with 4 test methods covering happy-path, limited-context, unresolved-customer, and blocked-write cases for cohort-scan scene
  </done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests for meeting-prep 4 case types</name>
  <files>tests/test_meeting_prep_scene.py</files>
  <read_first>
    - tests/test_meeting_prep_scene.py (existing tests to understand structure)
    - runtime/scene_runtime.py (run_meeting_prep_scene function, _render_meeting_prep_output)
  </read_first>
  <action>
    Add TestMeetingPrepRegression class to tests/test_meeting_prep_scene.py with 4 test methods:

    1. test_meeting_prep_happy_path_dispatch_and_result_shape: Create SceneRequest with scene_name="meeting-prep", customer_query="测试客户", inputs={"meeting_info": "Q2目标对齐会议"}. Call dispatch_scene(). Assert:
       - result.scene_name == "meeting-prep"
       - result.resource_status in ("live", "partial")
       - result.customer_status == "resolved"
       - result.context_status in ("complete", "partial")
       - result.write_ceiling in ("normal", "recommendation-only")
       - result.payload is not None and len(result.payload) > 0

    2. test_meeting_prep_limited_context_fallback_visible: Create SceneRequest with partial evidence. Assert:
       - result.context_status in ("partial", "minimal")
       - result.fallback_category in ("context", "none")
       - result.output_text is not None or result.recommendations is not None

    3. test_meeting_prep_unresolved_customer: Create SceneRequest with invalid customer. Assert:
       - result.customer_status == "not_found"
       - result.fallback_category == "customer"
       - result.write_ceiling == "recommendation-only"

    4. test_meeting_prep_blocked_write: Create SceneRequest with blocked write. Assert:
       - result.write_ceiling == "recommendation-only"
       - result.fallback_category in ("customer", "safety", "permission")
       - result.recommendations is not None and len(result.recommendations) > 0
       - result.payload is not None and "write_candidates" not in result.payload

    Use unittest assertions.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_meeting_prep_scene.TestMeetingPrepRegression -v 2>&1 | head -50</automated>
  </verify>
  <done>
    TestMeetingPrepRegression class exists in tests/test_meeting_prep_scene.py with 4 test methods covering happy-path, limited-context, unresolved-customer, and blocked-write cases for meeting-prep scene
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression tests for proposal 4 case types</name>
  <files>tests/test_proposal_scene.py</files>
  <read_first>
    - tests/test_proposal_scene.py (existing tests to understand structure, lines 1-150)
    - runtime/scene_runtime.py (run_proposal_scene function, _render_proposal_output)
  </read_first>
  <action>
    Add TestProposalRegression class to tests/test_proposal_scene.py with 4 test methods:

    1. test_proposal_happy_path_dispatch_and_result_shape: Create SceneRequest with scene_name="proposal", customer_query="测试客户", inputs={"proposal_type": "proposal", "goal": "Q2合作推进", "materials": []}. Call dispatch_scene(). Assert:
       - result.scene_name == "proposal"
       - result.resource_status in ("live", "partial")
       - result.customer_status == "resolved"
       - result.context_status in ("complete", "partial")
       - result.write_ceiling in ("normal", "recommendation-only")
       - result.payload is not None and len(result.payload) > 0

    2. test_proposal_limited_context_fallback_visible: Create SceneRequest with partial evidence. Assert:
       - result.context_status in ("partial", "minimal")
       - result.fallback_category in ("context", "none")
       - result.output_text is not None or result.recommendations is not None

    3. test_proposal_unresolved_customer: Create SceneRequest with invalid customer. Assert:
       - result.customer_status == "not_found"
       - result.fallback_category == "customer"
       - result.write_ceiling == "recommendation-only"

    4. test_proposal_blocked_write: Create SceneRequest with blocked write. Assert:
       - result.write_ceiling == "recommendation-only"
       - result.fallback_category in ("customer", "safety", "permission")
       - result.recommendations is not None and len(result.recommendations) > 0
       - result.payload is not None and "write_candidates" not in result.payload

    Use unittest assertions.
  </action>
  <verify>
    <automated>python3 -m unittest tests.test_proposal_scene.TestProposalRegression -v 2>&1 | head -50</automated>
  </verify>
  <done>
    TestProposalRegression class exists in tests/test_proposal_scene.py with 4 test methods covering happy-path, limited-context, unresolved-customer, and blocked-write cases for proposal scene
  </done>
</task>

</tasks>

<verification>
Run: python3 -m unittest tests.test_cohort_scan.TestCohortScanRegression tests.test_meeting_prep_scene.TestMeetingPrepRegression tests.test_proposal_scene.TestProposalRegression -v
All 12 tests pass (3 scenes x 4 case types).
</verification>

<success_criteria>
- TestCohortScanRegression: 4 tests pass
- TestMeetingPrepRegression: 4 tests pass
- TestProposalRegression: 4 tests pass
- All tests use SceneRequest -> dispatch_scene() -> SceneResult pattern
- Each test verifies scene_name, resource_status, customer_status, context_status, write_ceiling fields
- Blocked-write tests verify: write_ceiling == "recommendation-only", recommendations non-empty, write_candidates NOT in payload
- Fallback cases verify fallback_category is set to the correct per-scene category
</success_criteria>

<output>
After completion, create .planning/phases/21-validation-and-milestone-closure/21-02-SUMMARY.md
</output>
