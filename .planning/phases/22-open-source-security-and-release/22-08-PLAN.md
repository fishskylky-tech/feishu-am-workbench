---
phase: 22-open-source-security-and-release
plan: 08
type: execute
wave: 2
depends_on:
  - "22-02"
  - "22-04"
files_modified:
  - .gitignore
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "GitHub 仓库内容与 milestone 1.3 的 milestone/context 文件对照实现"
    - ".planning/phases/ 从 git 跟踪中移除"
    - "内部文档（docs/）已移入 .gitignore"
  artifacts:
    - path: ".gitignore"
      provides: "包含所有应归档文件的 gitignore"
      contains: "docs/"
    - path: ".gitignore"
      contains: ".planning/phases/"
  key_links: []
---

<objective>
对照 milestone 1.3 检查并修正 GitHub 仓库内容（Gap 4 major）。

用户原话: "当前 GitHub 仓库没有完全对照 milestone1.3 的 milestone 和 context 文件实现"

Gap 根因: GitHub 仓库内容与 milestone 1.3 规划不一致

**Execution Note:** This plan executes in Wave 2 after 22-02 and 22-04 complete, avoiding race condition with `git rm --cached` commands in active wave plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@MILESTONE-CONTEXT.md (v1.3 文件分类决策)
@REQUIREMENTS.md (v1.3 需求定义)

**NOTE:** This plan executes in Wave 2 after 22-02 and 22-04 complete, avoiding race condition with `git rm --cached` commands in active wave plans.
</context>

<tasks>

<task type="auto">
  <name>Task: 确认 .planning/phases/ 已从 git 跟踪移除</name>
  <files>.gitignore</files>
  <action>检查 .planning/phases/ 是否仍被 git 跟踪。如果 `git ls-files .planning/phases/` 仍有输出，执行 `git rm --cached -r .planning/phases/` 彻底移除跟踪，但保留本地文件。</action>
  <verify>git ls-files .planning/phases/ | wc -l</verify>
  <done>.planning/phases/ 不再被 git 跟踪（仅本地保留）</done>
</task>

<task type="auto">
  <name>Task: 将内部文档移入 .gitignore</name>
  <files>.gitignore</files>
  <action>在 .gitignore 中添加以下条目（如果不存在）：
- `docs/assessment-checklist.md`
- `docs/loading-strategy.md`

然后从 git 跟踪中移除：git rm --cached docs/assessment-checklist.md docs/loading-strategy.md

注：这两个是内部评估文档，不属于公共 skill 文档。</action>
  <verify>grep "docs/assessment-checklist.md\|docs/loading-strategy.md" .gitignore && git ls-files "docs/" | grep -c "assessment\|loading"</verify>
  <done>内部文档已移入 .gitignore，不再被 git 跟踪</done>
</task>

<task type="auto">
  <name>Task: 检查 milestone 1.3 要求的公共文档</name>
  <files>AGENTS.md, SECURITY-MODEL.md, WORKFLOW.md, CHANGELOG.md</files>
  <action>对照 MILESTONE-CONTEXT.md 中"应该公开"的文件列表，检查当前仓库是否缺少以下文件：

应该存在但可能缺失：
- AGENTS.md（专家角色定义）
- SECURITY-MODEL.md（安全模型说明）
- WORKFLOW.md（工作流说明）
- CHANGELOG.md（变更日志）

如果缺失，创建空文件或基础版本（内容可以从 SKILL.md 和 references/ 中提取关键信息），文件名标注 [NEEDS-REVIEW] 待后续完善。

如果已存在，确认内容合理。</action>
  <verify>ls -la AGENTS.md SECURITY-MODEL.md WORKFLOW.md CHANGELOG.md 2>/dev/null | grep -v "cannot access"</verify>
  <done>所有 milestone 1.3 要求的公共文档已就位</done>
</task>

<task type="auto">
  <name>Task: 验证 GitHub 仓库清洁度</name>
  <files>.gitignore</files>
  <action>执行最终验证，确认 GitHub 仓库内容符合 milestone 1.3：

1. 确认 .planning/ 不在 git 跟踪中：`git ls-files .planning/ | wc -l` 应为 0
2. 确认内部文档不在 git 跟踪中：`git ls-files docs/assessment-checklist.md docs/loading-strategy.md | wc -l` 应为 0
3. 确认必要公共文档存在：AGENTS.md、SECURITY-MODEL.md、WORKFLOW.md、CHANGELOG.md 至少存在骨架
4. 确认敏感文件不在跟踪：`.env`、`.secrets.baseline` 不在 git ls-files 中</action>
  <verify>echo "Checking git tracking..." && git ls-files .planning/ | wc -l && git ls-files "docs/assessment*" | wc -l && ls AGENTS.md SECURITY-MODEL.md WORKFLOW.md CHANGELOG.md 2>&1 | grep -v "cannot access"</verify>
  <done>GitHub 仓库符合 milestone 1.3 要求</done>
</task>

</tasks>

<verification>
- [ ] `git ls-files .planning/phases/ | wc -l` = 0
- [ ] `git ls-files "docs/assessment*" | wc -l` = 0
- [ ] AGENTS.md, SECURITY-MODEL.md, WORKFLOW.md, CHANGELOG.md 至少存在骨架文件
- [ ] `.env` 和 `.secrets.baseline` 不在 git ls-files 中
</verification>

<success_criteria>
GitHub 仓库内容与 milestone 1.3 的 milestone/context 文件对照实现：无多余跟踪文件，必要公共文档存在。
</success_criteria>

<output>
After completion, create `.planning/phases/22-open-source-security-and-release/22-08-SUMMARY.md`
</output>
