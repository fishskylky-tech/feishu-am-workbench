---
phase: 22-open-source-security-and-release
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - graphify-out/
autonomous: true
requirements:
  - CLEAN-01
  - CHECK-01
gap_closure: true

must_haves:
  truths:
    - "Git history contains NO customer names in any tracked file history"
    - "graphify-out/ is gitignored and removed from git tracking"
    - "All git tags verified clean or removed"
    - "docs/ is gitignored"
  artifacts:
    - path: ".gitignore"
      provides: "Updated gitignore with graphify-out/ and docs/ entries"
      contains: "graphify-out/"
    - path: ".gitignore"
      contains: "docs/"
key_links: []
---

<objective>
Close critical git history and gitignore gaps identified by OpenCode review:

1. Git history STILL contains customer names in OLD commits of tracked files (evals/evals.json before 8b8432c)
2. graphify-out/ directory is tracked and contains customer names from code analysis
3. Git tags not verified for sensitive content
4. docs/ directory not gitignored

Root cause: ASSESSMENT-REPORT incorrectly assessed git history risk as LOW, claiming customer names only exist in untracked directories. But evals/evals.json is tracked and its history contains customer names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-open-source-security-and-release/22-RESEARCH.md
@.planning/phases/22-open-source-security-and-release/22-VERIFICATION.md

**NOTE:** This plan is self-contained and does NOT depend on 22-01 outputs. Task 4 performs independent git history verification using the same commands documented in RESEARCH.md, making it independent of any filter-repo decision from 22-01.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add graphify-out/ to .gitignore and untrack</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
1. Check if graphify-out/ is tracked:
   git ls-files graphify-out/ | head -5

2. If tracked, untrack it:
   git rm --cached -r graphify-out/

3. Add to .gitignore:
   echo -e "\n# Graphify knowledge graph output (contains function names with customer identifiers)" >> .gitignore
   echo "graphify-out/" >> .gitignore

4. Verify:
   grep "graphify-out" .gitignore
   git ls-files graphify-out/ | wc -l  # should be 0
  </action>
  <acceptance_criteria>
    - graphify-out/ NOT in git ls-files (untracked)
    - graphify-out/ IN .gitignore
    - git grep customer names in graphify-out/ returns 0 from tracked files
  </acceptance_criteria>
  <verify>
    <automated>git ls-files graphify-out/ 2>/dev/null | wc -l</automated>
  </verify>
  <done>graphify-out/ gitignored and removed from tracking</done>
</task>

<task type="auto">
  <name>Task 2: Add docs/ to .gitignore</name>
  <files>.gitignore</files>
  <read_first>.gitignore</read_first>
  <action>
1. Add docs/ to .gitignore:
   grep -q "^docs/" .gitignore || echo -e "\n# Internal documentation" >> .gitignore
   grep -q "^docs$" .gitignore || echo "docs/" >> .gitignore

2. Verify:
   grep "docs/" .gitignore
  </action>
  <acceptance_criteria>
    - docs/ entry exists in .gitignore
  </acceptance_criteria>
  <verify>
    <automated>grep "docs/" .gitignore | head -3</automated>
  </verify>
  <done>docs/ gitignored</done>
</task>

<task type="auto">
  <name>Task 3: Verify git tags for sensitive content</name>
  <files>N/A</files>
  <read_first>N/A</read_first>
  <action>
1. List all tags:
   git tag -l

2. For each tag, check if it points to a commit containing customer names:
   ```bash
   for tag in $(git tag -l); do
     echo "=== Tag: $tag ==="
     git show "$tag":evals/evals.json 2>/dev/null | grep -c "unilever\|联合利华\|永和大王\|达美乐" || echo "0"
   done
   ```

3. If any tag has customer names, either:
   a) Delete the tag: git tag -d <tag> && git push origin :refs/tags/<tag>
   b) OR document why it's acceptable (e.g., pre-v1.0 archive)

4. Verify with:
   git log --all --source -S "unilever" --oneline | grep "refs/tags/" | head -5

**IMPORTANT:** This task verifies BOTH tag existence AND content cleanliness. The automated verify below checks for sensitive content in tags, not just count.
  </action>
  <acceptance_criteria>
    - All git tags verified for sensitive content
    - Any tags pointing to commits with customer names are either deleted or documented as acceptable
  </acceptance_criteria>
  <verify>
    <automated>
      # Check for customer names in tags (not just count tags)
      for tag in $(git tag -l); do
        count=$(git show "$tag":evals/evals.json 2>/dev/null | grep -c "unilever\|联合利华\|永和大王\|达美乐" || echo "0")
        if [ "$count" -gt 0 ]; then echo "SENSITIVE_IN_TAG: $tag"; fi
      done
      # If no SENSITIVE_IN_TAG output, all tags are clean
      echo "TAG_VERIFICATION_COMPLETE"
    </automated>
  </verify>
  <done>Git tags verified for sensitive content</done>
</task>

<task type="auto">
  <name>Task 4: Final comprehensive git history scan</name>
  <files>N/A</files>
  <read_first>N/A</read_first>
  <action>
IMPORTANT: This verifies whether git filter-repo is REQUIRED.

1. Check for customer names in tracked file HISTORY (not just working tree):
   git rev-list --all | xargs git grep "unilever\|联合利华" 2>/dev/null | grep -v "worktrees\|\.planning\|\.archive\|graphify-out" | head -10

2. Check specifically in evals/ history:
   git log --all -p -S "unilever" -- "evals/evals.json" 2>/dev/null | head -20

3. Decision matrix:
   - If grep finds customer names in tracked files (evals/, tests/, runtime/) → filter-repo REQUIRED
   - If grep only finds names in commit messages or .planning/ (untracked) → filter-repo NOT_REQUIRED

4. Document the decision clearly
  </action>
  <acceptance_criteria>
    - Clear documented decision: filter-repo REQUIRED or filter-repo NOT_REQUIRED
    - If REQUIRED: specific files/paths that need cleaning listed
    - If NOT_REQUIRED: rationale documented
  </acceptance_criteria>
  <verify>
    <automated>git rev-list --all | xargs git grep "unilever" 2>/dev/null | grep -v "worktrees\|\.planning\|\.archive\|graphify-out" | head -3</automated>
  </verify>
  <done>Git history scan complete with documented decision</done>
</task>

</tasks>

<verification>
- [ ] git ls-files graphify-out/ returns 0
- [ ] grep "graphify-out/" .gitignore returns 1+
- [ ] grep "docs/" .gitignore returns 1+
- [ ] Git tags verified (no SENSITIVE_IN_TAG output)
- [ ] Git history decision documented
</verification>

<success_criteria>
All gitignore gaps closed, git tags verified, git history decision documented.
</success_criteria>

<output>
After completion, create `.planning/phases/22-open-source-security-and-release/22-10-SUMMARY.md`
</output>
